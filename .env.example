# Database URL - defaults to SQLite for local development
# For PostgreSQL (production): postgresql+asyncpg://user:password@host:5432/akup
AKUP_DATABASE_URL=sqlite+aiosqlite:///./akup.db

# Echo SQL queries to stdout (for debugging)
AKUP_ECHO_SQL=false
